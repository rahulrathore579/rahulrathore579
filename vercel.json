{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "installCommand": "npm install",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/((?!.*\\..*).*)",
            "dest": "/index.html"
        }
    ]
}